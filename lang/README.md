# Language Packs

Languages are loaded from this folder.

To enable a language, keep its `*.js` file here and list it in `manifest.js`.
To disable a language, remove it from `manifest.js` and optionally delete its
pack file.

`pt.js` is the fallback language and should stay available.
